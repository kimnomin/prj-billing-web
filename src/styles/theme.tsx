import { theme as antdTheme } from "antd";

const theme = {
  algorithm: antdTheme.compactAlgorithm,
  token: {
    // global token
  },
  components: {
    // component token
  }
};

export default theme;